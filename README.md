# Universe Invoice PWA v0.7

核心更新：
- 手動、語音、掃描三個入口同一行；移除拍照／相片掃碼。
- 掃描預設 3×；成功時顯示綠色大 LOTNO，重複橙色，找不到紅色。
- 客戶欄位預設留空。
- Summary 移到加入貨品上方。
- 修正手機橫向溢出與圖片裁切。
- 圖片同變體優先採用沒有 `(1)` 的檔案。
- Confirm 可輸出可自由修改的 Invoice `.xlsx` 及 Remaining Stock `.xlsx`。

GitHub Pages：解壓後把全部檔案直接覆蓋 Repository 根目錄。

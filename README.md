# Universe Invoice PWA v0.9

本版集中修正：
- 手動、語音、掃描三個模式互斥高亮。
- 語音辨識結果立即寫入 LOTNO 欄位，不再自動清空。
- 掃描按一次即啟動相機；恢復中央窄長掃描框，預設 3×。
- 掃描只優先解 Code 128 / Code 39 / ITF，一維條碼成功後顯示大 LOTNO。
- 保留 Excel 輸出、Remaining Stock、圖片比例與無貨品分隔橫線修正。

GitHub Pages：解壓後把全部檔案直接覆蓋 Repository 根目錄。
